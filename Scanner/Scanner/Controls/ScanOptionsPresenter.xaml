<UserControl
    x:Name="Root"
    x:Class="Scanner.Controls.ScanOptionsPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactivity"
    xmlns:converters="using:Scanner.Views.Converters"
    mc:Ignorable="d"
    IsHitTestVisible="False"
    HighContrastAdjustment="None">


    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- XAML Converters -->
            <converters:EnumStringConverter x:Key="EnumStringConverter" />
            <converters:NegativeEnumIntComparisonConverter x:Key="NegativeEnumIntComparisonConverter" />
        </ResourceDictionary>
    </UserControl.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - -   CONTROL CONTENT  - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <Grid ColumnSpacing="8" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <FontIcon Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Glyph="&#xE7EA;" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontFamily="{StaticResource SymbolThemeFontFamily}" RenderTransformOrigin="0.5,0.5">
            <FontIcon.RenderTransform>
                <RotateTransform Angle="90" />
            </FontIcon.RenderTransform>
        </FontIcon>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Scan options" FontWeight="SemiBold" Foreground="{ThemeResource AccentTextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" />
        <ItemsControl Grid.Row="1" Grid.Column="1" Margin="-4,0,0,2" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="0" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            
            <ItemsControl.Items>
                <!-- Source mode -->
                <Border Padding="4,2,4,2" Background="Transparent" CornerRadius="8">
                    <TextBlock Text="{x:Bind ScanOptions.SourceMode, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" />
                </Border>

                <!-- File format -->
                <Border Padding="4,2,4,2" Background="Transparent" CornerRadius="8">
                    <TextBlock Text="{x:Bind ScanOptions.TargetFormat, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" />
                </Border>

                <!-- Color mode -->
                <Border x:Name="BorderColorMode" Padding="4,2,4,2" Background="Transparent" CornerRadius="8" x:Load="{x:Bind ScanOptions.SourceMode, Mode=OneWay, Converter={StaticResource NegativeEnumIntComparisonConverter}, ConverterParameter=1}">
                    <controls:SwitchPresenter Value="{x:Bind ScanOptions.ColorMode, Mode=OneWay, Converter={StaticResource EnumStringConverter}}">
                        <controls:Case Value="Color">
                            <FontIcon Glyph="&#xE790;" FontSize="12" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </controls:Case>

                        <controls:Case Value="Grayscale">
                            <Grid>
                                <FontIcon Glyph="&#xEA3B;" FontSize="12" FontFamily="{StaticResource SymbolThemeFontFamily}">
                                    <FontIcon.Foreground>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <LinearGradientBrush.GradientStops>
                                                <GradientStop Offset="0.0" Color="{ThemeResource TextFillColorSecondary}" />
                                                <GradientStop Offset="0.8" Color="Transparent" />
                                            </LinearGradientBrush.GradientStops>
                                        </LinearGradientBrush>
                                    </FontIcon.Foreground>
                                </FontIcon>
                                <FontIcon Glyph="&#xEA3A;" FontSize="12" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                            </Grid>
                        </controls:Case>

                        <controls:Case Value="Monochrome">
                            <FontIcon Glyph="&#xE7A1;" FontSize="12" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </controls:Case>
                    </controls:SwitchPresenter>
                </Border>

                <!-- Resolution -->
                <Border x:Name="BorderResolution" Padding="4,2,4,2" Background="Transparent" CornerRadius="8" x:Load="{x:Bind ScanOptions.SourceMode, Mode=OneWay, Converter={StaticResource NegativeEnumIntComparisonConverter}, ConverterParameter=1}">
                    <TextBlock Text="{x:Bind ScanOptions.Resolution.FriendlyShortText, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" />
                </Border>
            </ItemsControl.Items>
        </ItemsControl>
    </Grid>
</UserControl>
