<UserControl
    x:Class="Scanner.Controls.ScanMergePreviewBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactivity"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:converters="using:Scanner.Views.Converters"
    xmlns:strings="using:Scanner.Resources.Strings"
    xmlns:models="using:Scanner.Models"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    mc:Ignorable="d"
    IsFocusEngagementEnabled="True"
    UseSystemFocusVisuals="True"
    IsTabStop="True"
    AutomationProperties.Name="{x:Bind ScanMergeElement.ItemDescriptor, Mode=OneWay}"
    AutomationProperties.AutomationControlType="ListItem"
    ToolTipService.ToolTip="{x:Bind ScanMergeElement.ItemDescriptor, Mode=OneWay}">


    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <media:AttachedCardShadow x:Key="ShadowPage" Offset="0,2" BlurRadius="12" Color="Black" Opacity="0.04" CornerRadius="2" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <media:AttachedCardShadow x:Key="ShadowPage" Offset="0,2" BlurRadius="12" Color="Black" Opacity="0.3" CornerRadius="2" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <media:AttachedCardShadow x:Key="ShadowPage" Offset="0,0" BlurRadius="0" Color="Transparent" Opacity="0.0" CornerRadius="2" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            
            <!-- XAML Converters -->
            <converters:NegativeBoolConverter x:Key="NegativeBoolConverter"/>
            <converters:NegativeBoolVisibilityConverter x:Key="NegativeBoolVisibilityConverter"/>
            <converters:NotNullVisibilityConverter x:Key="NotNullVisibilityConverter"/>
            <converters:ScanMergeElementGlyphConverter x:Key="ScanMergeElementGlyphConverter"/>
            <converters:StringNotEmptyVisibilityConverter x:Key="StringNotEmptyVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - -   CONTROL CONTENT  - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <Grid Width="110" Height="110" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CornerRadius="{StaticResource ControlCornerRadius}">
        <Border Visibility="{x:Bind ScanMergeElement.IsPotentialPage, Mode=OneWay}" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="{StaticResource ControlCornerRadius}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="24" BorderBrush="{ThemeResource ContentDialogSeparatorBorderBrush}" BorderThickness="1" />
        <Border Visibility="{x:Bind ScanMergeElement.IsPotentialPage, Mode=OneWay, Converter={StaticResource NegativeBoolVisibilityConverter}}" CornerRadius="{StaticResource ControlCornerRadius}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <Frame HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="{StaticResource ControlCornerRadius}" ui:Effects.Shadow="{ThemeResource ShadowPage}" Visibility="{x:Bind ScanMergeElement.PreviewBitmapUri, Mode=OneWay, Converter={StaticResource NotNullVisibilityConverter}}">
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="3">
                <Image AutomationProperties.AccessibilityView="Raw">
                    <Image.Source>
                        <BitmapImage UriSource="{x:Bind ScanMergeElement.PreviewBitmapUri, Mode=OneWay}" DecodePixelType="Logical" DecodePixelWidth="110" />
                    </Image.Source>
                </Image>
            </Border>
        </Frame>

        <FontIcon Glyph="{x:Bind ScanMergeElement, Mode=OneWay, Converter={StaticResource ScanMergeElementGlyphConverter}}" FontSize="24" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" Visibility="{x:Bind ScanMergeElement.IsPotentialPage, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}" />

        <Grid x:Name="GridItemDescriptor" MinWidth="24" Height="24" Margin="0,0,0,4" Padding="8,0,8,0" HorizontalAlignment="Center" VerticalAlignment="Bottom" CornerRadius="4" Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}" Visibility="{x:Bind ScanMergeElement.ItemDescriptor, Mode=OneWay, Converter={StaticResource StringNotEmptyVisibilityConverter}}" x:Load="{x:Bind ScanMergeElement.IsPotentialPage, Mode=OneWay, Converter={StaticResource NegativeBoolConverter}}">
            <TextBlock Text="{x:Bind ScanMergeElement.ItemDescriptor, Mode=OneWay}" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" MaxLines="2" TextWrapping="WrapWholeWords" TextTrimming="Clip" />
        </Grid>
    </Grid>
</UserControl>
