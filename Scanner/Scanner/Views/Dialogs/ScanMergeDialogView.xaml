<ContentDialog
    x:Class="Scanner.Views.Dialogs.ScanMergeDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Scanner.ViewModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactivity"
    xmlns:interfaces="using:Scanner.Models.Interfaces"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:converters="using:Scanner.Views.Converters"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:storage="using:Windows.Storage"
    xmlns:teachingtips="using:Scanner.Views.TeachingTips"
    xmlns:strings="using:Scanner.Resources.Strings"
    xmlns:models="using:Scanner.Models"
    xmlns:customcontrols="using:Scanner.Controls"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    PrimaryButtonText="{x:Bind strings:Resources.Confirm}"
    CloseButtonText="{x:Bind strings:Resources.Cancel}"
    DefaultButton="Primary"
    PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
    FlowDirection="{x:Bind ViewModel.AccessibilityService.DefaultFlowDirection, Mode=OneWay}"
    PrimaryButtonCommand="{x:Bind ViewModel.AcceptCommand}"
    CloseButtonCommand="{x:Bind ViewModel.CancelCommand}">

    <ContentDialog.DataContext>
        <viewModels:ScanMergeDialogViewModel x:Name="ViewModel"/>
    </ContentDialog.DataContext>

    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Unloaded">
            <core:InvokeCommandAction Command="{x:Bind ViewModel.DisposeCommand}" />
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>


    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <ContentDialog.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///StylesDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- XAML Converters -->
            <converters:NegativeBoolVisibilityConverter x:Key="NegativeBoolVisibilityConverter"/>
            <converters:StringNotEmptyVisibilityConverter x:Key="StringNotEmptyVisibilityConverter"/>
            <converters:NotNullVisibilityConverter x:Key="NotNullVisibilityConverter"/>
            <converters:ScanMergeElementGlyphConverter x:Key="ScanMergeElementGlyphConverter"/>
            <converters:ToolTipConverter x:Key="ToolTipConverter"/>
        </ResourceDictionary>
    </ContentDialog.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  PAGE CONTENT  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <ContentDialog.Title>
        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
            <FontIcon Glyph="&#xEA3C;" FontFamily="{StaticResource SymbolThemeFontFamily}"  />
            <TextBlock Text="{x:Bind strings:Resources.DialogScanMergeHeading}" AutomationProperties.HeadingLevel="Level1" />
        </StackPanel>
    </ContentDialog.Title>
    
    <Grid ColumnSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" VerticalAlignment="Top">
            <TextBlock Text="{x:Bind strings:Resources.ScanMergeExplanation}" Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,24"/>
        </ScrollViewer>
        <NumberBox Grid.Row="1" Grid.Column="0" SpinButtonPlacementMode="Inline" Minimum="1" Maximum="{x:Bind ViewModel.TotalNumberOfPages, Mode=OneWay}" Value="{x:Bind ViewModel.StartPageNumber, Mode=TwoWay}" MinWidth="100" AutomationProperties.IsRequiredForForm="True" AutomationProperties.LandmarkType="Form" Loaded="NumberBox_Loaded">
            <NumberBox.Header>
                <StackPanel Orientation="Horizontal">
                    <StackPanel x:Name="StackPanelHeaderStartPage" Orientation="Horizontal" Visibility="{x:Bind ViewModel.ReversePages, Mode=OneWay, Converter={StaticResource NegativeBoolVisibilityConverter}}">
                        <FontIcon Glyph="&#xE819;" FontSize="16" Margin="0,0,8,0" MinWidth="20" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        <TextBlock Text="{x:Bind strings:Resources.ScanMergeStartPage}" Style="{StaticResource BodyTextBlockStyle}" />
                    </StackPanel>
                    <StackPanel x:Name="StackPanelHeaderLastPage" Orientation="Horizontal" Visibility="{x:Bind ViewModel.ReversePages, Mode=OneWay}">
                        <FontIcon Glyph="&#xE81B;" FontSize="16" Margin="0,0,8,0" MinWidth="20" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        <TextBlock Text="{x:Bind strings:Resources.ScanMergeLastPage}" Style="{StaticResource BodyTextBlockStyle}" />
                    </StackPanel>
                </StackPanel>
            </NumberBox.Header>
        </NumberBox>
        <NumberBox Grid.Row="1" Grid.Column="1" SpinButtonPlacementMode="Inline" Minimum="0" Maximum="{x:Bind ViewModel.MaxSkippablePages, Mode=OneWay}" Value="{x:Bind ViewModel.SkipPages, Mode=TwoWay}" MinWidth="100" AutomationProperties.IsRequiredForForm="True" Loaded="NumberBox_Loaded">
            <NumberBox.Header>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xEB9D;" FontSize="16" MinWidth="20" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    <TextBlock Text="{x:Bind strings:Resources.ScanMergeInBetweenPages}" Style="{StaticResource BodyTextBlockStyle}" />
                </StackPanel>
            </NumberBox.Header>
        </NumberBox>
        <ToggleButton x:Name="ButtonReverseOrder" Grid.Row="1" Grid.Column="2" ToolTipService.ToolTip="{x:Bind strings:Resources.ScanMergeReverseOrderToolTip}" VerticalAlignment="Bottom" Height="{ThemeResource TextControlThemeMinHeight}" IsChecked="{x:Bind ViewModel.ReversePages, Mode=TwoWay}" AutomationProperties.Name="{x:Bind ButtonReverseOrder, Converter={StaticResource ToolTipConverter}}">
            <FontIcon Glyph="&#xE13C;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
        </ToggleButton>
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Text="{x:Bind strings:Resources.ScanMergePreviewHeading}" Style="{StaticResource BaseTextBlockStyle}" Margin="0,32,0,12" AutomationProperties.LandmarkType="Navigation" AutomationProperties.HeadingLevel="Level2" />
        
        <!-- Preview -->
        <ScrollViewer Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="300" Margin="-24,0,-24,-24" BorderBrush="{ThemeResource ContentDialogSeparatorBorderBrush}" BorderThickness="0,1,0,0">
            <ItemsRepeater x:Name="ItemsRepeaterScanMergeElements" HorizontalAlignment="Center" VerticalAlignment="Top" ItemsSource="{x:Bind ViewModel.MergePreview, Mode=OneWay}" Margin="24,16,24,24" MinHeight="200">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MaximumRowsOrColumns="4" Orientation="Horizontal" MinRowSpacing="8" MinColumnSpacing="4"/>
                </ItemsRepeater.Layout>

                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:ScanMergeElement">
                        <customcontrols:ScanMergePreviewBlock ScanMergeElement="{x:Bind Mode=OneWay}" />
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</ContentDialog>
