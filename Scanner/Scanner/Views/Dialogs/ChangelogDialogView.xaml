<ContentDialog
    x:Class="Scanner.Views.Dialogs.ChangelogDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Scanner.ViewModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactivity"
    xmlns:interfaces="using:Scanner.Models.Interfaces"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:local="using:Scanner"
    xmlns:strings="using:Scanner.Resources.Strings"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    PrimaryButtonText="{x:Bind strings:Resources.SupportDevelopment}"
    PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
    CloseButtonText="{x:Bind strings:Resources.Close}"
    FlowDirection="{x:Bind AccessibilityService.DefaultFlowDirection, Mode=OneWay}">

    
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <ContentDialog.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///StylesDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <!-- Light accent variants read well against the dark dialog background. -->
                    <LinearGradientBrush x:Key="HeaderAccentGradientBrush" StartPoint="0,0" EndPoint="1.5,0.5">
                        <GradientStop Color="{ThemeResource SystemAccentColorLight2}" Offset="0" />
                        <GradientStop Color="{ThemeResource SystemAccentColorLight1}" Offset="1.0" />
                    </LinearGradientBrush>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <!-- Darker accent variants keep the headings readable on the light background. -->
                    <LinearGradientBrush x:Key="HeaderAccentGradientBrush" StartPoint="0,0" EndPoint="1.5,0.5">
                        <GradientStop Color="{ThemeResource SystemAccentColorDark1}" Offset="0" />
                        <GradientStop Color="{ThemeResource SystemAccentColorDark2}" Offset="1.0" />
                    </LinearGradientBrush>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <LinearGradientBrush x:Key="HeaderAccentGradientBrush" StartPoint="0,0" EndPoint="1.5,0.5">
                        <GradientStop Color="{ThemeResource SystemColorHighlightColor}" Offset="0" />
                        <GradientStop Color="{ThemeResource SystemColorHighlightColor}" Offset="1.0" />
                    </LinearGradientBrush>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <x:Double x:Key="ContentDialogMinWidth">400</x:Double>
            <x:Double x:Key="ContentDialogMinHeight">400</x:Double>
        </ResourceDictionary>
    </ContentDialog.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  PAGE CONTENT  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <ScrollViewer Margin="-24" Padding="8,0,16,0" HorizontalScrollMode="Disabled" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical" Spacing="12" Margin="16,24,16,24">
            <StackPanel Orientation="Vertical" Spacing="4" Margin="0,0,0,12">
                <TextBlock Text="{x:Bind strings:Resources.ChangelogDialogHeading}" FontSize="20" Style="{StaticResource BaseTextBlockStyle}" AutomationProperties.HeadingLevel="Level1" />
                <TextBlock Text="v4.0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource CaptionTextBlockStyle}" AutomationProperties.HeadingLevel="Level2" />
            </StackPanel>
            
            <Grid MinHeight="124" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="124" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Frame Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{StaticResource CardStrokeColorDefaultBrush}" BorderThickness="1.25">
                    <ui:Effects.Shadow>
                        <media:AttachedCardShadow Offset="0,8" BlurRadius="16" Color="Black" Opacity="0.02" CornerRadius="8" />
                    </ui:Effects.Shadow>

                    <FontIcon Glyph="&#xE794;" FontSize="32" FontFamily="{StaticResource SymbolThemeFontFamily}" VerticalAlignment="Center" Foreground="{ThemeResource HeaderAccentGradientBrush}" />
                </Frame>
                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="4" Margin="0,4,0,4" VerticalAlignment="Center">
                    <TextBlock Text="A new, familiar design" FontSize="16" Style="{StaticResource BaseTextBlockStyle}" Foreground="{ThemeResource HeaderAccentGradientBrush}" TextWrapping="WrapWholeWords" AutomationProperties.HeadingLevel="Level2" />
                    <TextBlock Text="Enjoy less friction and more personalization with page thumbnails, scanning from the page list, and resizable panes." Style="{StaticResource BodyTextBlockStyle}" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Grid>

            <Grid MinHeight="124" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="124" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Frame Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{StaticResource CardStrokeColorDefaultBrush}" BorderThickness="1.25">
                    <ui:Effects.Shadow>
                        <media:AttachedCardShadow Offset="0,8" BlurRadius="16" Color="Black" Opacity="0.02" CornerRadius="8" />
                    </ui:Effects.Shadow>

                    <FontIcon Glyph="&#xEC4A;" FontSize="32" FontFamily="{StaticResource SymbolThemeFontFamily}" VerticalAlignment="Center" Foreground="{ThemeResource HeaderAccentGradientBrush}" />
                </Frame>
                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="4" Margin="0,4,0,4" VerticalAlignment="Center">
                    <TextBlock Text="Faster and more efficient than ever" FontSize="16" Style="{StaticResource BaseTextBlockStyle}" Foreground="{ThemeResource HeaderAccentGradientBrush}" TextWrapping="WrapWholeWords" AutomationProperties.HeadingLevel="Level2" />
                    <TextBlock Text="Scan without limits and effortlessly manage huge projects thanks to the completely overhauled resource management." Style="{StaticResource BodyTextBlockStyle}" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Grid>

            <Grid MinHeight="124" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="124" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Frame Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{StaticResource CardStrokeColorDefaultBrush}" BorderThickness="1.25">
                    <ui:Effects.Shadow>
                        <media:AttachedCardShadow Offset="0,8" BlurRadius="16" Color="Black" Opacity="0.02" CornerRadius="8" />
                    </ui:Effects.Shadow>

                    <FontIcon Glyph="&#xEA90;" FontSize="32" FontFamily="{StaticResource SymbolThemeFontFamily}" VerticalAlignment="Center" Foreground="{ThemeResource HeaderAccentGradientBrush}" />
                </Frame>
                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="4" Margin="0,4,0,4" VerticalAlignment="Center">
                    <TextBlock Text="Advanced PDF tools" FontSize="16" Style="{StaticResource BaseTextBlockStyle}" Foreground="{ThemeResource HeaderAccentGradientBrush}" TextWrapping="WrapWholeWords" AutomationProperties.HeadingLevel="Level2" />
                    <TextBlock Text="Scan to existing PDFs, make pages searchable with OCR, split into individual files, add images, and try the new single-page PDF format." Style="{StaticResource BodyTextBlockStyle}" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Grid>

            <Grid MinHeight="124" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="124" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Frame Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{StaticResource CardStrokeColorDefaultBrush}" BorderThickness="1.25">
                    <ui:Effects.Shadow>
                        <media:AttachedCardShadow Offset="0,8" BlurRadius="16" Color="Black" Opacity="0.02" CornerRadius="8" />
                    </ui:Effects.Shadow>

                    <FontIcon Glyph="&#xEA86;" FontSize="32" FontFamily="{StaticResource SymbolThemeFontFamily}" VerticalAlignment="Center" Foreground="{ThemeResource HeaderAccentGradientBrush}" />
                </Frame>
                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="4" Margin="0,4,0,4" VerticalAlignment="Center">
                    <TextBlock Text="Highly-requested features" FontSize="16" Style="{StaticResource BaseTextBlockStyle}" Foreground="{ThemeResource HeaderAccentGradientBrush}" TextWrapping="WrapWholeWords" AutomationProperties.HeadingLevel="Level2" />
                    <TextBlock Text="Select a save location after scanning, undo/redo, filters for all scanners, paper size selection, lossless editing, subfolder generation, templates, and many more!" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
