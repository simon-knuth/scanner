<Page
    x:Class="Scanner.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Scanner.Views"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Scanner.ViewModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:converters="using:Scanner.Views.Converters"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:ShellViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loading">
            <core:InvokeCommandAction Command="{x:Bind ViewModel.ViewLoadingCommand}" CommandParameter="{x:Bind DispatcherQueue}" />
        </core:EventTriggerBehavior>
        <core:EventTriggerBehavior EventName="Unloaded">
            <core:InvokeCommandAction Command="{x:Bind ViewModel.DisposeCommand}" />
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>


    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///StylesDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- XAML Converters -->
            <converters:NegativeBoolConverter x:Key="NegativeBoolConverter" />
        </ResourceDictionary>
    </Page.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  PAGE CONTENT  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <Grid x:Name="GridRoot" SizeChanged="GridRoot_SizeChanged" Loaded="GridRoot_Loaded">
        <!-- VisualStates -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup" CurrentStateChanged="VisualStateGroup_CurrentStateChanged">
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        
                    </VisualState.StateTriggers>

                    <Storyboard>

                    </Storyboard>

                    <VisualState.Setters>
                        <Setter Target="StackPanelTitlebarButtonsRightExpanded.Visibility" Value="Collapsed" />
                        <Setter Target="ButtonTitlebarMore.Visibility" Value="Visible" />
                        <Setter Target="ColumnDefinitionFirst.MaxWidth" Value="2000" />
                        <Setter Target="ColumnDefinitionMiddle.Width" Value="0" />
                        <Setter Target="ScanOptionsView.CanExpandPageList" Value="False" />
                        <Setter Target="BorderScanOptions.Margin" Value="0,152,0,0" />
                        <Setter Target="BorderScanOptions.Padding" Value="0,0,0,0" />
                        <Setter Target="BorderScanOptions.Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
                        <Setter Target="BorderScanOptions.BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <Setter Target="BorderScanActionsView.Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
                    </VisualState.Setters>
                </VisualState>
                
                <VisualState x:Name="VisualStateNarrowNoProject">
                    <VisualState.StateTriggers>
                        
                    </VisualState.StateTriggers>

                    <Storyboard>

                    </Storyboard>

                    <VisualState.Setters>
                        <Setter Target="StackPanelTitlebarButtonsRightExpanded.Visibility" Value="Collapsed" />
                        <Setter Target="ButtonTitlebarMore.Visibility" Value="Visible" />
                        <Setter Target="ColumnDefinitionFirst.MaxWidth" Value="2000" />
                        <Setter Target="ColumnDefinitionMiddle.Width" Value="0" />
                        <Setter Target="BorderProjectView.Visibility" Value="Collapsed" />
                        <Setter Target="ScanOptionsView.CanExpandPageList" Value="False" />
                        <Setter Target="BorderScanOptions.Padding" Value="0,8,0,0" />
                        <Setter Target="BorderScanOptions.Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
                        <Setter Target="BorderScanOptions.BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <Setter Target="BorderScanActionsView.Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateDefault">
                    <VisualState.StateTriggers>

                    </VisualState.StateTriggers>

                    <Storyboard>

                    </Storyboard>

                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateDefaultNoProject">
                    <VisualState.StateTriggers>

                    </VisualState.StateTriggers>

                    <Storyboard>

                    </Storyboard>

                    <VisualState.Setters>
                        <Setter Target="BorderProjectView.Visibility" Value="Collapsed" />
                        <Setter Target="BorderScanOptions.Padding" Value="0,8,0,0" />
                        <Setter Target="ScanOptionsView.CanExpandPageList" Value="False" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>

                    </VisualState.StateTriggers>

                    <Storyboard>

                    </Storyboard>

                    <VisualState.Setters>
                        <Setter Target="ColumnDefinitionLast.Width" Value="348" />
                        <Setter Target="BorderProjectView.(Grid.Column)" Value="2" />
                        <Setter Target="BorderProjectView.(Grid.RowSpan)" Value="2" />
                        <Setter Target="ProjectView.IsExpanded" Value="True" />
                        <Setter Target="BorderScanOptions.Padding" Value="0,8,0,0" />
                        <Setter Target="BorderScanOptions.Visibility" Value="Visible" />
                        <Setter Target="ScanOptionsView.CanExpandPageList" Value="False" />
                        <Setter Target="ScanOptionsView.Visibility" Value="Visible" />
                        <Setter Target="ScanActionsView.AreScanOptionsVisible" Value="False" />
                        <Setter Target="BorderEditor.BorderThickness" Value="1,1,1,0" />
                        <Setter Target="BorderEditor.CornerRadius" Value="8,8,0,0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <!-- Titlebar -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition x:Name="ColumnDefinitionTitlebarInset" Width="164" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="/Assets/Logo small.svg" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <StackPanel x:Name="StackPanelTitlebarButtonsLeft" Grid.Column="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button ToolTipService.ToolTip="New project" Style="{StaticResource TitlebarButtonStyle}" IsEnabled="{x:Bind ViewModel.CanStartNewProject, Mode=OneWay}" Command="{x:Bind ViewModel.TryCloseProjectAsyncCommand}">
                        <FontIcon Glyph="&#xE8A5;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />

                        <Button.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="N" />
                        </Button.KeyboardAccelerators>
                    </Button>
                    <Button ToolTipService.ToolTip="Open" Style="{StaticResource TitlebarButtonStyle}" IsEnabled="{x:Bind ViewModel.ProjectService.IsProcessRunning, Mode=OneWay, Converter={StaticResource NegativeBoolConverter}}">
                        <FontIcon Glyph="&#xE838;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />

                        <Button.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="O" />
                        </Button.KeyboardAccelerators>
                    </Button>
                    <Button ToolTipService.ToolTip="Save" Style="{StaticResource TitlebarButtonStyle}" IsEnabled="{x:Bind ViewModel.CanSaveProject, Mode=OneWay}">
                        <FontIcon Glyph="&#xE74E;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />

                        <Button.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="S" />
                        </Button.KeyboardAccelerators>
                    </Button>
                    <Button ToolTipService.ToolTip="Undo" Style="{StaticResource TitlebarButtonStyle}" IsEnabled="{x:Bind ViewModel.CanUndo, Mode=OneWay}" Command="{x:Bind ViewModel.TryUndoAsyncCommand}">
                        <FontIcon Glyph="&#xE7A7;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />

                        <Button.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="Z" />
                        </Button.KeyboardAccelerators>
                    </Button>
                    <Button ToolTipService.ToolTip="Redo" Style="{StaticResource TitlebarButtonStyle}" IsEnabled="{x:Bind ViewModel.CanRedo, Mode=OneWay}" Command="{x:Bind ViewModel.TryRedoAsyncCommand}">
                        <FontIcon Glyph="&#xE7A6;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />

                        <Button.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="Y" />
                        </Button.KeyboardAccelerators>
                    </Button>
                </StackPanel>
                <StackPanel x:Name="StackPanelTitlebarButtonsRight" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel x:Name="StackPanelTitlebarButtonsRightExpanded" Orientation="Horizontal" Spacing="8">
                        <Button x:Name="ButtonHistory" ToolTipService.ToolTip="History" Style="{StaticResource TitlebarButtonStyle}" Click="ButtonHistory_Click">
                            <FontIcon Glyph="&#xE81C;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </Button>
                        <Button ToolTipService.ToolTip="Help" Style="{StaticResource TitlebarButtonStyle}">
                            <FontIcon Glyph="&#xE897;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </Button>
                        <Button ToolTipService.ToolTip="Support the development" Style="{StaticResource TitlebarButtonStyle}">
                            <FontIcon Glyph="&#xED56;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </Button>
                        <Button ToolTipService.ToolTip="Discover more" Style="{StaticResource TitlebarButtonStyle}">
                            <FontIcon Glyph="&#xECAD;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </Button>
                        <Button x:Name="ButtonSettings" ToolTipService.ToolTip="Settings" Style="{StaticResource TitlebarButtonStyle}" Command="{x:Bind ViewModel.ShowSettingsCommand}" RightTapped="ButtonSettings_RightTapped">
                            <AnimatedIcon Height="16">
                                <AnimatedIcon.Source>
                                    <animatedvisuals:AnimatedSettingsVisualSource />
                                </AnimatedIcon.Source>
                                <AnimatedIcon.FallbackIconSource>
                                    <SymbolIconSource Symbol="Setting" />
                                </AnimatedIcon.FallbackIconSource>
                            </AnimatedIcon>

                            <FlyoutBase.AttachedFlyout>
                                <Flyout>
                                    <StackPanel Orientation="Vertical" Spacing="4" Width="300" Height="400">
                                        <StackPanel.ChildrenTransitions>
                                            <TransitionCollection>
                                                <RepositionThemeTransition IsStaggeringEnabled="False" />
                                            </TransitionCollection>
                                        </StackPanel.ChildrenTransitions>
                                        
                                        <controls:SettingsExpander Header="Dialogs">
                                            <controls:SettingsExpander.HeaderIcon>
                                                <FontIcon Glyph="&#xE768;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                            </controls:SettingsExpander.HeaderIcon>

                                            <controls:SettingsExpander.Items>
                                                <controls:SettingsCard x:Name="SettingsCardDebugDialogSaveChanges" Header="Save changes" IsClickEnabled="True" Click="SettingsCardDebugDialogSaveChanges_Click">
                                                    <controls:SettingsCard.HeaderIcon>
                                                        <FontIcon Glyph="&#xE74E;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                                    </controls:SettingsCard.HeaderIcon>
                                                </controls:SettingsCard>
                                            </controls:SettingsExpander.Items>
                                        </controls:SettingsExpander>
                                        <controls:SettingsExpander Header="Notifications">
                                            <controls:SettingsExpander.HeaderIcon>
                                                <FontIcon Glyph="&#xE768;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                            </controls:SettingsExpander.HeaderIcon>

                                            <controls:SettingsExpander.Items>
                                                <controls:SettingsCard>
                                                    <controls:SettingsCard.Content>
                                                        <StackPanel Orientation="Vertical" Spacing="12">
                                                            <TextBox Header="Title" Text="{x:Bind DebugNotification.Title, Mode=TwoWay}" />
                                                            <TextBox Header="Message" Text="{x:Bind DebugNotification.Message, Mode=TwoWay}" />
                                                            <ComboBox x:Name="ComboBoxDebugNotificationsSeverity" Header="Severity" SelectedIndex="0">
                                                                <ComboBoxItem Content="Informational" />
                                                                <ComboBoxItem Content="Success" />
                                                                <ComboBoxItem Content="Warning" />
                                                                <ComboBoxItem Content="Error" />
                                                            </ComboBox>
                                                            <Button x:Name="ButtonDebugNotification" Content="Show notification" Click="ButtonDebugNotification_Click" />
                                                        </StackPanel>
                                                    </controls:SettingsCard.Content>
                                                </controls:SettingsCard>
                                            </controls:SettingsExpander.Items>
                                        </controls:SettingsExpander>
                                    </StackPanel>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                        </Button>
                    </StackPanel>
                    <Button x:Name="ButtonTitlebarMore" ToolTipService.ToolTip="More" Style="{StaticResource TitlebarButtonStyle}" Visibility="Collapsed">
                        <FontIcon Glyph="&#xE712;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />

                        <Button.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <MenuFlyoutItem x:Name="MenuFlyoutItemHistory" Text="History" Click="MenuFlyoutItemHistory_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE81C;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Help">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE897;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Support the development">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xED56;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Discover more">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xECAD;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Settings" Command="{x:Bind ViewModel.ShowSettingsCommand}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE713;" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </Grid>
            
            <!-- Content -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="ColumnDefinitionFirst" Width="4*" MaxWidth="348" />
                    <ColumnDefinition x:Name="ColumnDefinitionMiddle" Width="6*" />
                    <ColumnDefinition x:Name="ColumnDefinitionLast" Width="0" />
                </Grid.ColumnDefinitions>

                <Border x:Name="BorderProjectView" Grid.Row="0" Grid.Column="0" Margin="0,8,0,0">
                    <views:ProjectView x:Name="ProjectView" CanExpandPageList="{x:Bind ShowExpandButtonInProjectView, Mode=OneWay}" ExpandPageListRequested="ExpandPageListRequested" IsExpandedChanged="ProjectView_IsExpandedChanged" />
                </Border>
                <Border x:Name="BorderScanOptions" Grid.Row="0" Grid.Column="0" Padding="0,152,0,0" BorderThickness="0,1,0,0" ui:UIElementExtensions.ClipToBounds="True">
                    <animations:Implicit.ShowAnimations>
                        <animations:OpacityAnimation Duration="0:0:0.2" To="1.0" />
                        <animations:TranslationAnimation Duration="0:0:0.25" To="0,0" EasingMode="EaseInOut" />
                    </animations:Implicit.ShowAnimations>

                    <animations:Implicit.HideAnimations>
                        <animations:OpacityAnimation Duration="0:0:0.2" To="0.0" />
                        <animations:TranslationAnimation Duration="0:0:0.25" To="0,64" EasingMode="EaseInOut" />
                    </animations:Implicit.HideAnimations>

                    <views:ScanOptionsView x:Name="ScanOptionsView" CanExpandPageList="True" ExpandPageListRequested="ExpandPageListRequested" PropertyChanged="ScanOptionsView_PropertyChanged" />
                </Border>
                <Border x:Name="BorderScanActionsView" Grid.Row="1" Grid.Column="0">
                    <views:ScanActionsView x:Name="ScanActionsView" Height="72" ScanOptions="{x:Bind ScanOptionsView.ScanOptions, Mode=OneWay}" ExpandScanOptionsRequested="ScanActionsView_ExpandScanOptionsRequested" AreScanOptionsVisibleChanged="ScanActionsView_AreScanOptionsVisibleChanged" />
                </Border>
                <Line Grid.Row="1" Grid.Column="0" X1="0" X2="1" Stretch="Fill" Stroke="{ThemeResource DividerStrokeColorDefault}" StrokeThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" Visibility="{x:Bind ShowScanActionsDivider, Mode=OneWay}" />

                <Border x:Name="BorderEditor" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1,1,0,0" CornerRadius="8,0,0,0">
                    <views:EditorView />
                </Border>
            </Grid>
            
            <!-- Notifications -->
            <Frame Grid.Row="1" Grid.Column="0" Margin="24" CornerRadius="8" Background="{ThemeResource SystemControlAcrylicElementBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Visibility="{x:Bind InfoBarNotifications.IsOpen, Mode=OneWay}" ui:VisualExtensions.Scale="0,0" ui:VisualExtensions.NormalizedCenterPoint="0.5,1">
                <ui:Effects.Shadow>
                    <media:AttachedCardShadow Offset="0,4" BlurRadius="24" Color="Black" Opacity="0.1" CornerRadius="8" />
                </ui:Effects.Shadow>

                <animations:Implicit.ShowAnimations>
                    <animations:OpacityAnimation Duration="0:0:0.5" From="0.0" To="1.0" />
                    <animations:ScaleAnimation Duration="0:0:0.5" From="0,0" To="1,1" EasingMode="EaseOut"/>
                </animations:Implicit.ShowAnimations>

                <InfoBar x:Name="InfoBarNotifications" MaxWidth="448" CornerRadius="8">
                    <interactivity:Interaction.Behaviors>
                        <behaviors:StackedNotificationsBehavior x:Name="NotificationQueue" />
                    </interactivity:Interaction.Behaviors>
                </InfoBar>
            </Frame>
        </Grid>
    </Grid>
</Page>
