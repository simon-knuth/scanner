<settings:SettingsPage
    x:Class="Scanner.Views.Settings.SettingsViewPersonalization"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Scanner.ViewModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactivity"
    xmlns:interfaces="using:Scanner.Models.Interfaces"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:converters="using:Scanner.Views.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:settings="using:Scanner.Views.Settings"
    xmlns:globalization="using:Windows.Globalization"
    xmlns:strings="using:Scanner.Resources.Strings"
    mc:Ignorable="d">


    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <settings:SettingsPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///StylesDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- XAML Converters -->
            <converters:NotNullBoolConverter x:Key="NotNullBoolConverter" />
        </ResourceDictionary>
    </settings:SettingsPage.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  PAGE CONTENT  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <Grid x:Name="GridRoot">
        <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Spacing="4" Padding="32">
                <TextBlock Text="{x:Bind strings:Resources.SettingsPersonalizationHeading}" Margin="0,0,0,16" FontSize="24" Style="{StaticResource BaseTextBlockStyle}" />
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsThemeHeading}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE793;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>

                    <ComboBox SelectedIndex="{x:Bind ViewModel.SettingAppTheme, Mode=TwoWay}">
                        <ComboBox.Items>
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsThemeSystem}" />
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsThemeLight}" />
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsThemeDark}" />
                        </ComboBox.Items>
                    </ComboBox>
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsExpandPageListHeading}" Description="{x:Bind strings:Resources.SettingsExpandPageListBody}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE7EA;" FontFamily="{StaticResource SymbolThemeFontFamily}" RenderTransformOrigin="0.5,0.5">
                            <FontIcon.RenderTransform>
                                <RotateTransform Angle="270" />
                            </FontIcon.RenderTransform>
                        </FontIcon>
                    </controls:SettingsCard.HeaderIcon>

                    <ToggleSwitch IsOn="{x:Bind ViewModel.SettingsService.SettingExpandPageList, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsMeasurementUnitsHeading}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xED5E;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>

                    <ComboBox SelectedIndex="{x:Bind ViewModel.SettingMeasurementUnits, Mode=TwoWay}">
                        <ComboBox.Items>
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsMeasurementUnitsMetric}" />
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsMeasurementUnitsImperialUS}" />
                        </ComboBox.Items>
                    </ComboBox>
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsEditorOrientationHeading}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xECE7;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>

                    <ComboBox SelectedIndex="{x:Bind ViewModel.SettingEditorOrientation, Mode=TwoWay}">
                        <ComboBox.Items>
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsEditorOrientationHorizontal}" />
                            <ComboBoxItem Content="{x:Bind strings:Resources.SettingsEditorOrientationVertical}" />
                        </ComboBox.Items>
                    </ComboBox>
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsMirrorLayout}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE90D;" MirroredWhenRightToLeft="True" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>

                    <ToggleSwitch IsOn="{x:Bind ViewModel.SettingsService.SettingMirrorAppLayout, Mode=TwoWay}" />
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsLanguageHeading}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xF2B7;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>

                    <ComboBox ItemsSource="{x:Bind ViewModel.AvailableAppLanguages}" SelectedItem="{x:Bind ViewModel.SettingAppLanguage, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:DisplayedLanguage">
                                <TextBlock Text="{x:Bind FriendlyName}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsAnimations}" Description="{x:Bind strings:Resources.SettingsAnimationsDescription}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE805;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>

                    <ToggleSwitch IsOn="{x:Bind ViewModel.SettingsService.SettingAnimations, Mode=TwoWay}" />
                </controls:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</settings:SettingsPage>
