<settings:SettingsPage
    x:Class="Scanner.Views.Settings.SettingsViewFeedback"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Scanner.ViewModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactivity"
    xmlns:interfaces="using:Scanner.Models.Interfaces"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:converters="using:Scanner.Views.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:settings="using:Scanner.Views.Settings"
    xmlns:strings="using:Scanner.Resources.Strings"
    mc:Ignorable="d">


    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  RESOURCES  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <settings:SettingsPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///StylesDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- XAML Converters -->
            <converters:NotNullBoolConverter x:Key="NotNullBoolConverter" />
        </ResourceDictionary>
    </settings:SettingsPage.Resources>


    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - -  PAGE CONTENT  - - - - - - - - - - - - - - - - -->
    <!-- - - - - - - - - - - - - - - - - - - -  - - - - - - - - - - - - - - - - - - - - -->
    <Grid x:Name="GridRoot">
        <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Spacing="4" Padding="32">
                <TextBlock Text="{x:Bind strings:Resources.SettingsFeedbackHeading}" Margin="0,0,0,16" FontSize="24" Style="{StaticResource BaseTextBlockStyle}" />
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsFeedbackSendMessageHeading}" Description="{x:Bind strings:Resources.SettingsFeedbackSendMessageBody}" IsClickEnabled="True" Command="{x:Bind ViewModel.ShowFeedbackDialogCommand}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE8BD;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>
                <controls:SettingsCard Header="{x:Bind strings:Resources.SettingsFeedbackRateInStore}" IsClickEnabled="True" Command="{x:Bind ViewModel.RateInStoreCommand}">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE734;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>
                <Grid>
                    <controls:SettingsCard x:Name="SettingsCardGitHub" Header="{x:Bind strings:Resources.GitHub}" Description="{x:Bind strings:Resources.SettingsGitHubBody}" IsClickEnabled="True" Click="SettingsCardGitHub_Click">
                        <controls:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="    " FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </controls:SettingsCard.HeaderIcon>

                        <controls:SettingsCard.ActionIcon>
                            <FontIcon Glyph="&#xE8A7;" FontFamily="{StaticResource SymbolThemeFontFamily}" />
                        </controls:SettingsCard.ActionIcon>
                    </controls:SettingsCard>

                    <!-- IconSourceElement doesn't support SVGs atm, can't directly use an SVG as HeaderIcon -->
                    <Image Source="/Assets/GitHub/GitHub_Invertocat.svg" Width="20" Height="20" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" IsHitTestVisible="False" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</settings:SettingsPage>
