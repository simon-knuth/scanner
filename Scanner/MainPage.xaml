<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Scanner"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:Custom="using:Microsoft.Toolkit.Uwp.UI.Controls.TextToolbarButtons"
    x:Class="Scanner.MainPage"
    mc:Ignorable="d" 
    SizeChanged="Page_SizeChanged">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColumnLeft" Width="3*" MinWidth="300" MaxWidth="400"/>
            <ColumnDefinition x:Name="ColumnRight" Width="7*"/>
        </Grid.ColumnDefinitions>

        <Frame Grid.Column="0" Background="{ThemeResource SystemControlAcrylicWindowBrush}">
            <Grid HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition MaxHeight="75"/>
                </Grid.RowDefinitions>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="0" Margin="0,32,0,0">
                    <StackPanel Name="StackPanelLeft" Orientation="Vertical" Padding="24,0,24,6" HorizontalAlignment="Stretch">
                        <TextBlock Text="Scanner" Style="{ThemeResource SubheaderTextBlockStyle}" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                            <TextBlock Text="Source" Style="{StaticResource BaseTextBlockStyle}"/>
                            <TextBlock Name="TextBlockRefreshHint" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.5" Visibility="Collapsed"/>
                            <ProgressRing Name="ProgressRingRefresh" IsActive="False" Opacity="0.5" Background="Transparent" Margin="12,0,0,0"/>
                        </StackPanel>
                        <ComboBox IsEnabled="False" Name="ComboBoxScanners" CornerRadius="3" ItemsSource="{x:Bind scannerList}" HorizontalAlignment="Stretch" Margin="0,6,0,0" SelectionChanged="ComboBoxScanners_SelectionChanged"/>
                        <StackPanel x:Name="StackPanelSource" Orientation="Vertical" Margin="0,12,0,0">
                            <RadioButton Name="RadioButtonSourceAutomatic" GroupName="RadioButtonGroupSource" IsEnabled="False" HorizontalAlignment="Stretch" Checked="RadioButtonSourceChanged">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Automatic configuration"/>
                                    <TextBlock Text=" (Recommended)" Opacity="0.5"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Name="RadioButtonSourceFlatbed" GroupName="RadioButtonGroupSource" Content="Flatbed" IsEnabled="False" HorizontalAlignment="Stretch" Checked="RadioButtonSourceChanged"/>
                            <RadioButton Name="RadioButtonSourceFeeder" GroupName="RadioButtonGroupSource" Content="Feeder" IsEnabled="False" HorizontalAlignment="Stretch" Checked="RadioButtonSourceChanged"/>
                        </StackPanel>
                        <StackPanel Name="StackPanelColor" Orientation="Vertical" Margin="0,48,0,0" Visibility="Collapsed">
                            <TextBlock Text="Color Mode" Style="{StaticResource BaseTextBlockStyle}"/>
                            <RadioButton Name="RadioButtonColorModeColor" GroupName="RadioButtonGroupColor" Content="Color" IsEnabled="False" Margin="0,6,0,0" HorizontalAlignment="Stretch"/>
                            <RadioButton Name="RadioButtonColorModeGrayscale" GroupName="RadioButtonGroupColor" Content="Grayscale" IsEnabled="False" HorizontalAlignment="Stretch"/>
                            <RadioButton Name="RadioButtonColorModeMonochrome" GroupName="RadioButtonGroupColor" Content="Monochrome" IsEnabled="False" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                        <StackPanel Name="StackPanelResolution" Orientation="Vertical" Margin="0,48,0,0" Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Resolution" Style="{StaticResource BaseTextBlockStyle}"/>
                                <TextBlock Text=" (affects file size)" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.5"/>
                            </StackPanel>
                            <ComboBox Name="ComboBoxResolution" Margin="0,6,0,0" CornerRadius="3" HorizontalAlignment="Stretch" IsEnabled="False"/>
                        </StackPanel>
                        <TextBlock Text="Save result as" Style="{StaticResource BaseTextBlockStyle}" Margin="0,48,0,0"/>
                        <ComboBox Name="ComboBoxType" Margin="0,6,0,0" CornerRadius="3" HorizontalAlignment="Stretch" IsEnabled="False" SelectedIndex="0">
                            <ComboBoxItem Content="JPG image (prioritize file size)"/>
                            <ComboBoxItem Content="PNG image (prioritize quality)"/>
                        </ComboBox>
                    </StackPanel>
                </ScrollViewer>
                <Grid Grid.Row="1" Padding="24,12,24,12" VerticalAlignment="Stretch">
                    <Button Name="ButtonScan" Content="Scan" CornerRadius="3" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{ThemeResource ButtonRevealStyle}" Padding="16,8,16,8" Background="{ThemeResource SystemControlHighlightAccentRevealBackgroundBrush}" FontWeight="SemiBold" MinWidth="100" IsEnabled="False" Foreground="{ThemeResource SystemColorHighlightTextColor}" Click="ButtonScan_Click"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <Button x:Name="ButtonSettings" ToolTipService.ToolTip="Settings" Style="{ThemeResource TextBlockButtonStyle}" Padding="8,8,8,8" Margin="0,0,12,0">
                            <SymbolIcon Symbol="Setting"/>
                        </Button>
                        <Button x:Name="ButtonRecents" ToolTipService.ToolTip="Previous scans" Style="{ThemeResource TextBlockButtonStyle}" Padding="8,8,8,8" Click="Button_Click">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE81C;"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Frame>

        <RelativePanel Grid.Column="1" Name="RelativePanelRight">
            <Frame Name="TitleBarFrame" Background="{ThemeResource SystemControlAcrylicWindowBrush}" Height="50" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
            <Frame Name="LeftFrame" Background="{ThemeResource SystemControlAcrylicWindowBrush}" Width="50" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True"/>
            <controls:DropShadowPanel   BlurRadius="30" ShadowOpacity="0.5" OffsetX="40" OffsetY="40" OffsetZ="0"
                                        Color="Black" Padding="0,2,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                                        HorizontalContentAlignment="Stretch" Name="DropShadowPanelRight" RelativePanel.AlignTopWithPanel="True"
                                        RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"
                                        RelativePanel.AlignBottomWithPanel="True">
                <Grid Name="GridPanelRight" CornerRadius="10, 0, 0, 0" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="32,32,0,0" VerticalAlignment="Stretch">
                    <StackPanel x:Name="StackPanelTextRight" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12,0,12,0" Opacity="0.0">
                        <StackPanel.OpacityTransition>
                            <ScalarTransition/>
                        </StackPanel.OpacityTransition>

                        <TextBlock Text="Select a scanner to start" Style="{ThemeResource SubheaderTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,12" TextWrapping="WrapWholeWords"/>
                        <TextBlock TextWrapping="WrapWholeWords" Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.75">
                                <Span xml:space="preserve"><Run>If your scanner doesn't show up, you might need to add it in the </Run><Hyperlink UnderlineStyle="None" x:Name="HyperlinkSettings" NavigateUri="ms-settings:printers">settings app</Hyperlink><Run> first.</Run></Span>
                        </TextBlock>
                    </StackPanel>
                    <Image x:Name="ImageScanViewer"/>
                </Grid>
            </controls:DropShadowPanel>
        </RelativePanel>

    </Grid>
</Page>
